import Product from '../models/Product';

module.exports = {
  createProduct: async (req, res) => {
    //Creo un producto
  },

  getProductById: async (req, res) => {
    //Trae uno o varios productos segundo su ID. Puedo hacerlo con query o params.
  },
}